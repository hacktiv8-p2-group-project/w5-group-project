//@ts-check
$(document).ready(() => {
    auth()
})

const base_url = "http://localhost:3000/"

function auth() {
    if (!localStorage.getItem("access_token")) {
        //
    } else {
        //
    }
}
